## Transpile js files in client to TypeScript (TODO)

Get dependencies:
`npm install`

Build and watch:
`npm run watch`

Run:
`npm run gui`

Run unit tests:
`npm test`

Continuously run tests:
`npm run testMon`

Build installer:
`npm run dist`
