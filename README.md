# Typify

A desktop app for converting JavaScript files to Typescript. 

Get dependencies:
`npm install`

Build and watch:
`npm run watch`

Run:
`npm start`

Run unit tests:
`npm test`

Build installer:
`npm run dist`
